---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overseerr-storage-data
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
