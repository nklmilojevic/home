---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tautulli-data
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 2Gi
