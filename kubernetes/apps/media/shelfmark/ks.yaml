---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app shelfmark
spec:
  components:
    - ../../../../components/volsync
  targetNamespace: media
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  dependsOn:
    - name: volsync
      namespace: security
    - name: rook-ceph-cluster
      namespace: rook-ceph
  path: ./kubernetes/apps/media/shelfmark/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: home-kubernetes
    namespace: flux-system
  wait: false
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substitute:
      VOLSYNC_SCHEDULE: "15 0 * * *"
      VOLSYNC_LOCAL_SCHEDULE: "15 3 * * *"
      APP: *app
      VOLSYNC_CLAIM: shelfmark
      VOLSYNC_CAPACITY: 1Gi
