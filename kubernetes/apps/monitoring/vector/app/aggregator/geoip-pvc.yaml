---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geoip-data
  namespace: monitoring
  annotations:
    volumeType: local
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 50Gi
