---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zwavejs-storage-data
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
