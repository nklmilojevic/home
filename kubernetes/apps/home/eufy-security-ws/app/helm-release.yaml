---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: eufy-security-ws
  namespace: home
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=http://bjw-s.github.io/helm-charts/
      chart: app-template
      version: 3.0.4
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: uninstall
      retries: 3
  values:
    eufy-security-ws:
      main:
        replicas: 1
        strategy: RollingUpdate

        containers:
          app:
            image:
              repository: bropat/eufy-security-ws
              tag: 1.8.0
            envFrom:
              - secretRef:
                  name: eufy-secrets
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
    service:
      app:
        controller: eufy-security-ws
        ports:
          http:
            port: 3000
