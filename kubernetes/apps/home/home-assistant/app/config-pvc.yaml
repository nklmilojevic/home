---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hass-data
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 2Gi
