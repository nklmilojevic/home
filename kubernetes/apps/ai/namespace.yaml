---
apiVersion: v1
kind: Namespace
metadata:
  name: ai
  labels:
    kustomize.toolkit.fluxcd.io/prune: disabled
    # Required for DRA ResourceClaimTemplates that use adminAccess (iGPU sharing)
    resource.kubernetes.io/admin-access: "true"
