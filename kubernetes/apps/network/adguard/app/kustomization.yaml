apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./pvc.yaml
  - ./helmrelease.yaml
configMapGenerator:
  - name: adguard-config
    files:
      - resources/AdGuard.yaml
generatorOptions:
  disableNameSuffixHash: true
